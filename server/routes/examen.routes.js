const examenController = require("../controllers/examen.controller");

module.exports = (app) =>{
    // app.get("/ruta", examenController.<metodo a usar>);  Muestra informacion
    // app.post("/ruta", examenController.<metodo a usar>); Guardar informacion
    // app.put("/ruta", examenController.<metodo a usar>); Actualiza informacion
    // app.delete("/ruta", examenController.<metodo a usar>); Borra informacion
}